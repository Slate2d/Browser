fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.0
jinja2==3.1.4
psutil==5.9.8
websockets==12.0
# Prefer 'patchright' if you use it; the worker will fallback to 'playwright' if patchright is unavailable.
playwright==1.47.0